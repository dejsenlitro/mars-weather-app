{
  "name": "mars-weather-app",
  "version": "0.0.0",
  "description": "App to receive weather data for Mars from NASA api",
  "main": "index.js",
  "scripts": {
    "start": "rm -rf build && tsc && node ./build/index.js",
    "debug": "NODE_ENV=development rm -rf build && tsc && node --inspect node_modules/.bin/ts-node src"
  },
  "author": "Arin Hlaj",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "express-graphql": "^0.9.0",
    "graphql": "^14.5.8"
  },
  "devDependencies": {
    "@types/express": "^4.17.1",
    "ts-node": "^8.4.1"
  }
}
